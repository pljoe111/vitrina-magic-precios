
## Set Admin Password

**What**: Update the `ADMIN_PASSWORD` backend secret to the new value `qhi5h4po`.

**How**: Use the secret management tool to overwrite the existing `ADMIN_PASSWORD` secret with the new value.

**After**: You'll be able to log in at `/alchem-admin` with:
- Username: `alchem`
- Password: `qhi5h4po`
